## 1. UI スタイル更新（ホワイト基調テーマ）

- [x] 1.1 `style.css` にホワイト基調のカラースキーム変数を定義（背景白、テキスト濃色、アクセント色）
- [x] 1.2 既存ダークテーマ色変数をホワイトテーマの色に置き換え
- [x] 1.3 ボタン・コントロール要素にアクセント色（ブルー、グリーン等）を適用
- [x] 1.4 プレイヤーUI（再生・一時停止ボタン、スライダー）のスタイルを白テーマに統一
- [x] 1.5 記事タイトル表示エリアのCSSを修正（`word-break: break-word`、`line-height: 1.6` 以上）
- [x] 1.6 ハイライト色を白テーマに適した色に変更（薄いブルー #E8F4FF または黄色等）
- [x] 1.7 すべてのテキスト色が背景色に対して十分なコントラストを持つことを確認

## 2. JavaScript アプリロジック更新（新規JSON形式対応）

- [x] 2.1 `app.js` の新規JSON構造（`main_news`、`category_news`）への対応を確認
- [x] 2.2 主要ニュース（`main_news`）を最初のセクションに表示するロジック実装
- [x] 2.3 カテゴリ別ニュース（`category_news`）を複数セクション（タブまたはアコーディオン）として表示するロジック実装
- [x] 2.4 既存の記事表示・クリック機能がカテゴリ別データに対応することを確認
- [x] 2.5 音声再生・ハイライト機能が新規JSON構造と互換性があることをテスト

## 3. Python スクリプト更新（複数カテゴリニュース取得）

- [x] 3.1 `fetch_news.py` に複数NHKニュースRSSフィード定義を追加（主要`cat0`、カテゴリ別`cat1`, `cat2`等）
- [x] 3.2 複数RSSフィードへの並列リクエスト機能を実装（`concurrent.futures` または非同期ライブラリ使用）
- [x] 3.3 主要フィードから10件、カテゴリフィードから各5件を取得する件数制限ロジック実装
- [x] 3.4 各ニュース記事のURL先から `newspaper3k` で本文全文を抽出する既存ロジックを確認・保持
- [x] 3.5 Gemini APIへの送信前にニュースデータがカテゴリ別に整理されていることを確認
- [x] 3.6 新規JSON出力形式（`main_news` と `category_news` キー）での生成ロジック実装
- [x] 3.7 `data/news.json` が新規形式で正しく出力されることをテスト

## 4. データ構造・フォーマット検証

- [x] 4.1 新規`data/news.json` の構造が仕様（`main_news`、`category_news`）に準拠することを確認
- [x] 4.2 各ニュースアイテムに必須フィールド（`id`, `title`, `summary`, `content`, `translation`, `timestamp`）が含まれることを確認
- [x] 4.3 サンプルJSONデータを作成・検証（手動テスト用）

## 5. ブラウザ互換性・UI テスト

- [ ] 5.1 主要ブラウザ（Chrome、Firefox、Safari）でホワイトテーマが正しく表示されることを確認
- [ ] 5.2 長い記事タイトル（日本語混在）が複数行で読みやすく折り返されることを確認
- [ ] 5.3 再生ボタン・速度スライダー・音声選択ドロップダウンが白テーマで見やすいことを確認
- [ ] 5.4 ハイライト表示が薄いブルー/黄色で視認しやすいことを確認
- [ ] 5.5 主要ニュース・カテゴリ別ニュースの表示切り替えが正常に動作することを確認

## 6. GitHub Actions ワークフロー確認・更新

- [ ] 6.1 `.github/workflows/` のニュース取得スケジュール設定を確認
- [ ] 6.2 更新されたPythonスクリプト（`fetch_news.py`）がワークフロー内で正しく実行されることを確認
- [ ] 6.3 新規JSON出力形式が GitHub Pages にデプロイされることを確認
- [ ] 6.4 GitHub Actions ログで多重フィード取得の実行状況を検証

## 7. 統合テスト・デプロイ

- [ ] 7.1 全ブラウザ環境での統合テスト実施（UI + ニュース表示 + 音声再生機能）
- [ ] 7.2 ローカル環境で `npm start` / Pythonスクリプト実行後、期待通りに機能することを確認
- [ ] 7.3 新規JSON形式でのニュース表示、カテゴリ別フィルタリング動作確認
- [ ] 7.4 本番環境へのデプロイ（GitHub Pages、リポジトリ更新）
- [ ] 7.5 デプロイ後、本番環境での動作確認（新規ニュース表示、UI表示、音声再生）
